pipeline {
    agent { label 'AG-1' }

    stages {
        stage('Build') {
            steps {
                echo 'Building the project... Sumanth'
            }
        }

        stage('Test') {
            steps {
                echo 'Running tests...'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying application...'
            }
        }
    }
}
