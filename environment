pipeline {
    agent {label 'AG-1'}

    environment {
        PROJECT = "Expense"
    }

    stages {
        stage('Test') {
            steps {
              script{
                sh 'echo Project name is $PROJECT'
              }
            }
        }
    }
}
