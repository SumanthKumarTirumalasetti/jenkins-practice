pipeline {
    agent { label 'AG-1' }

    stages {
        stage('Test') {
            steps {
                sh 'hostname'
                sh 'whoami'
                sh 'df -h'
            }
        }
    }
}
